const config = {
    email: 'behrooziali28@gmail.com',
    password: '',
    wallet: 'exchangeEarnedWallet', // exchangeWallet / exchangeEarnedWallet
    block_check_time: 1000 // time to delay in checking blocks data in mili second
};

exports.config = config;